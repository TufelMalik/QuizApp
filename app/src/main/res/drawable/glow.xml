<!-- res/drawable/glow.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/main_color" /> <!-- Change this color to the desired glow color -->
            <corners android:radius="8dp" /> <!-- Adjust the radius as per your CardView's corner radius -->
        </shape>
    </item>
</layer-list>
